MPICC = mpicc

CFLAGS = -fopenmp

build:
	$(MPICC) $(CFLAGS) -c main.c
	$(MPICC) $(CFLAGS) -c funcs.c
	$(MPICC) main.o funcs.o -o exec -lgomp -lm

clean:
	rm -f *.o ./exec

run:
	mpiexec -n 4 ./exec < ./inputs/input-medium.txt

